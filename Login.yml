info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{local}}auth/login"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "admin123"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        //setup untuk token otomatis
        const data = res.getBody();
        bru.setEnvVar("token", data.data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
