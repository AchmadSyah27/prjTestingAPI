info:
  name: GetStudent
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}students"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
