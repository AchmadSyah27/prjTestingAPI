info:
  name: DeleteStudentByCreatedDate
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{local}}students/date/2026-06-12"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
