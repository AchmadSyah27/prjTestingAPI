info:
  name: DeleteStudent
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}students/name/Siti Rahayu"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
